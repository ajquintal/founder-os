---
name: content-engine
description: >-
  Stand up a content system ONCE — design the 3–5 pillars, the format mix, the one-asset-to-many
  repurposing pipeline, and the weekly cadence structure — engineered so every asset compounds toward
  the offer ladder, not vanity reach. The one-time build/design; `/content-cadence` runs the ongoing
  rhythm once it exists. Use when standing up or fixing organic content or designing a content
  calendar. Triggers: "content engine", "content system", "content pillars", "repurpose",
  "content calendar", "what should I post".
---

# /content-engine — Content Engine

Stand up the content system once: the 3–5 pillars, format mix, repurposing pipeline, and weekly cadence structure that turn one anchor asset into many and point every piece at a rung of the offer ladder. This is the one-time build/design — `/content-cadence` runs the steady-state rhythm once the system exists. Reads `/positioning` (the SOURCE of the pillars), `/offer-architect` (the ladder), and `/market-scan` (the wedge).

## Load first
1. `founder-profile/PROFILE.md`, `founder-profile/voice-and-brand.md`, `founder-profile/operating-playbooks.md` (GTM, retention), `founder-profile/guardrails.md` (drafts only)
2. Active `ventures/<slug>/venture-context.md`
3. Latest `/positioning` output — the SOURCE of the content pillars (pull them from here; don't invent them) — plus `/offer-architect` (the ladder) and `/market-scan` (the wedge), if they exist.

## Method (full detail + worked example in `references/method.md`)
1. **Fix inputs:** ICP, the offer-ladder rungs, and the *one* channel the ICP already lives on. One anchor channel — not six. (Guards: too many fronts.)
2. **Set 3–5 pillars (pulled from `/positioning`):** each answers a specific JTBD or buyer objection, feeds one named offer rung, and carries a proof source (data, case, mechanism) — not a vibe.
3. **Choose the anchor format:** one long-form asset per week (essay or video). Everything else is derived from it. Capture once.
4. **Build the repurposing pipeline:** one anchor → atomized derivatives per channel (short posts, email, clips, carousel). Never write net-new per channel.
5. **Set the weekly cadence:** batch — plan → produce anchor → atomize → schedule (draft) → engage/measure. Name an owner per slot.
6. **Instrument:** one compounding metric per pillar (list adds, signups, rung conversion) — never likes.

## Output contract
- **Pillars table:** pillar · JTBD/objection · offer rung fed · anchor format · proof source · primary metric.
- **Repurposing pipeline:** 1 anchor → N derivatives, each mapped to a channel, drawn as the fan-out flow.
- **Weekly cadence table:** day · slot · action · output · owner (batched, not daily net-new).
- The one pillar to lead with now, and why (usually the wedge).

## Rules
- Drafts only — queue for approval; never auto-publish.
- Voice per `founder-profile/voice-and-brand.md`: clinical, precise, active. Use power words; never the banned list.
- Pillars come from `/positioning` — pull the messaging pillars, don't invent them; then sharpen and map each to an offer rung.
- Every pillar maps to an offer rung. No content that doesn't compound toward revenue.
- LYV firewall: never cross-source LYV audience or assets.
- Respect venture claims guardrails (EE: no medical claims; never "physician review"/"medical team").
- Never imply a live team/cohort dashboard pre-contract — B2B/operator pillars reference a quarterly report, not a live dashboard.

## Examples & evals
- `references/method.md` — full method + a worked example (Executive Edge).
- `evals/evals.md` — 3 cases with expected outputs.
