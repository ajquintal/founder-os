// Extends Vitest's `expect` with jest-dom matchers (e.g. toBeInTheDocument),
// so component tests work out of the box. Loaded via vite.config.ts setupFiles.
import '@testing-library/jest-dom/vitest'
